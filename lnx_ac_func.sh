#!/bin/bash
#script to display the functionality of the function

simple_func(){
  #We are now inside the function body
  echo "We are now inside the function"
}

#Now we are going to execute the function
simple_func
echo "Printing this outside the function"
